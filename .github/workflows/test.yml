name: Build

on:
  push:
  workflow_dispatch:

jobs:

  ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: make consumer-feature-examples
    
  mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - run: make build
      - run: make consumer-feature-examples
    

